# ============================================================================
# Conduit - Environment Variables
# ============================================================================
# Copy this file to the appropriate location:
#   Backend:  cp .env.example backend/.env
#   Frontend: cp .env.example frontend/.env.local
#
# Only the variables relevant to each service need to be set.
# ============================================================================

# --- Frontend (prefix with NEXT_PUBLIC_ for client-side access) -------------

# Privy authentication (https://dashboard.privy.io)
NEXT_PUBLIC_PRIVY_APP_ID=
NEXT_PUBLIC_PRIVY_CLIENT_ID=
PRIVY_APP_SECRET=

# Backend API URL (defaults to http://localhost:3001 if not set)
NEXT_PUBLIC_API_URL=http://localhost:3001

# --- Backend ----------------------------------------------------------------

# REST server port (defaults to 3001)
PORT=3001

# SQLite database path (defaults to ./data/conduit.db)
CONDUIT_DB_PATH=./data/conduit.db

# Encryption key for agent wallet private keys (32 bytes hex = 64 chars)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WALLET_ENCRYPTION_KEY=

# --- 0G Chain (Newton Testnet, chainId 16602) --------------------------------

# RPC endpoint
ZEROG_RPC_URL=https://evmrpc-testnet.0g.ai

# Deployer/funder private key (needs testnet 0G tokens from https://faucet.0g.ai)
ZEROG_PRIVATE_KEY=

# Deployed contract addresses on 0G Newton testnet
AGENT_NFT_ADDRESS=
CONDUIT_ADDRESS=

# --- Base Mainnet — revenue settlement + gas funding -----------------------

# Base mainnet RPC endpoint
BASE_RPC_URL=https://mainnet.base.org

# Server wallet private key for auto-funding agent wallets
BASE_PRIVATE_KEY=

# ERC-8021 Builder Code (register at https://base.dev)
BASE_BUILDER_CODE=

# Gas monitor thresholds
MIN_BALANCE_ETH=0.001
FUNDING_AMOUNT_ETH=0.01
MIN_SERVER_BALANCE_ETH=0.02
GAS_MONITOR_INTERVAL_MS=300000

# --- Hedera (Testnet) — optional, stub implementation -----------------------

HEDERA_NETWORK=testnet
HEDERA_ACCOUNT_ID=
HEDERA_PRIVATE_KEY=
