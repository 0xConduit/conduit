# 0G Chain — Newton testnet
# RPC endpoint (public, no key needed)
ZEROG_RPC_URL=https://evmrpc-testnet.0g.ai

# Your wallet private key — must hold testnet 0G tokens for gas
# Get tokens: https://faucet.0g.ai
ZEROG_PRIVATE_KEY=0x

# AgentNFT contract address — printed by `forge script` after deploy
# Deploy: cd contracts/conduit && forge script script/Conduit.s.sol --rpc-url https://evmrpc-testnet.0g.ai --broadcast --private-key $ZEROG_PRIVATE_KEY
AGENT_NFT_ADDRESS=0x

# Optional: Conduit registry contract address (for future use)
# CONDUIT_ADDRESS=0x
