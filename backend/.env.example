# REST server port (defaults to 3001)
PORT=3001

# SQLite database path (defaults to ./data/conduit.db)
CONDUIT_DB_PATH=./data/conduit.db

# ═══════════════════════════════════════════════════════════════════════════
# 0G Chain — Newton testnet
# RPC endpoint (public, no key needed)
ZEROG_RPC_URL=https://evmrpc-testnet.0g.ai

# Your wallet private key — must hold testnet 0G tokens for gas
# Get tokens: https://faucet.0g.ai
ZEROG_PRIVATE_KEY=0x

# Deployed contract addresses on 0G Newton testnet
# Printed by `forge script` after deploy:
#   cd contracts/conduit && forge script script/Conduit.s.sol --rpc-url https://evmrpc-testnet.0g.ai --broadcast --private-key $ZEROG_PRIVATE_KEY
AGENT_NFT_ADDRESS=
CONDUIT_ADDRESS=

# Encryption key for agent wallet private keys (32 bytes hex = 64 chars)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
WALLET_ENCRYPTION_KEY=

# ═══════════════════════════════════════════════════════════════════════════
# Base Mainnet Configuration — Self-Sustaining Autonomous Agents
# ═══════════════════════════════════════════════════════════════════════════

# Base mainnet RPC endpoint
# Public RPC: https://mainnet.base.org
# Or use a provider like Alchemy, Infura, QuickNode, etc.
BASE_RPC_URL=https://mainnet.base.org

# Server wallet private key for auto-funding agent wallets (self-sustaining)
# This wallet should hold ETH on Base mainnet to fund agent gas costs
# Keep this secure and funded with sufficient ETH
BASE_PRIVATE_KEY=0x

# ERC-8021 Builder Code from base.dev (required for transaction attribution)
# Register at https://base.dev → Settings → Builder Code
# This enables analytics tracking and eligibility for builder rewards
BASE_BUILDER_CODE=

# Gas monitoring configuration (self-sustaining features)
# Minimum balance threshold before auto-funding agents (in ETH)
MIN_BALANCE_ETH=0.001

# Amount to fund agents when balance is low (in ETH)
FUNDING_AMOUNT_ETH=0.01

# Minimum server wallet balance required to enable auto-funding (in ETH)
# Should be at least FUNDING_AMOUNT_ETH + gas costs for multiple transactions
MIN_SERVER_BALANCE_ETH=0.02

# Gas monitor check interval in milliseconds (default: 5 minutes)
GAS_MONITOR_INTERVAL_MS=300000

# ═══════════════════════════════════════════════════════════════════════════
# Hedera (Testnet) — optional, stub implementation
# ═══════════════════════════════════════════════════════════════════════════

HEDERA_NETWORK=testnet
HEDERA_ACCOUNT_ID=
HEDERA_PRIVATE_KEY=
